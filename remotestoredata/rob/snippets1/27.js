{"js":"\nfunction randomColor () {\n  return Math.round(Math.random()*255)+','+\n  Math.round(Math.random()*255)+','+\n  Math.round(Math.random()*255);\n}\n\nvar a = [];\nfunction addItem(x,y,elem) {\n  var n = parseInt(elem.innerText || elem.textContent);\n  if(isNaN(n))\n    n = null;\n  if(!a[x])\n    a[x] = [];\n  a[x][y] = n; \n}\nvar color = randomColor();\n\n\nSimplePath.getElementsByPath(\n  aardvark.pathFromEditor(),\n  null, \n  function (elem, i) {\n    var path = SimplePath.getPath(elem);\n    var row = path[path.length-2].num;\n    var col  = path[path.length-1].num;\n    if(row > 2 && row < 18 && col>1){\n      elem.style.border = '2px dashed rgb(' +color+')';\n      elem.style.background = 'rgba('+color+',.2)';\n      addItem(row-3,col-2,elem);\n    }\n    else {\n      elem.style.border = '';\n      elem.style.background = ''\n    }\n  }\n);\nlog(a);\n\n","name":"election scraper","num":27}